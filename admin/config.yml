backend:
  name: git-gateway
  branch: main

media_folder: "assets/img"

collections:
  - name: "editoriales"
    label: "Editoriales"
    folder: "content/editoriales"
    create: true
    fields:
      - {label: "Título", name: "title", widget: "string"}
      - {label: "Contenido", name: "body", widget: "markdown"}
